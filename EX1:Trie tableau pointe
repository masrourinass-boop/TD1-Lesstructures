void trier(Point t[], int n) {
    int i,j;
    Point tmp;

    for(i=0;i<n;i++)
        for(j=i+1;j<n;j++)
            if(distance(t[i]) > distance(t[j])) {
                tmp = t[i];
                t[i] = t[j];
                t[j] = tmp;
            }
}
